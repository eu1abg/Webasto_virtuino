# LabadaSto: Система управления предпусковым подогревателем Webasto с ESP32

![ESP32](https://img.shields.io/badge/ESP32-3.00-blue.svg)
![MQTT](https://img.shields.io/badge/MQTT-поддержка-green.svg)
![AutoOTA](https://img.shields.io/badge/AvtoFotaNew-1.00-yellow.svg)
![License](https://img.shields.io/badge/Лицензия-MIT-orange.svg)

**Умная система управления предпусковым подогревателем Webasto** на базе ESP32 с поддержкой дистанционного управления через MQTT, автоматического поддержания температуры и нескольких режимов работы.

## 📋 Содержание

1. [Основные возможности](#-основные-возможности)
2. [Аппаратные требования](#аппаратные-требования)
3. [Схема подключения](#схема-подключения)
4. [Установка и настройка](#установка-и-настройка)
5. [Режимы работы](#режимы-работы)
6. [MQTT API](#mqtt-api)
7. [Меню устройства](#меню-устройства)
8. [Автоматическое обновление](#автоматическое-обновление)
9. [Структура проекта](#структура-проекта)
10. [Настройки по умолчанию](#настройки-по-умолчанию)
11. [Лицензия](#лицензия)

## ✨ Основные возможности

- **Автоматическое управление** температурой в салоне автомобиля
- **Четыре режима работы**: полное отключение, подготовка, прогрев двигателя, ручное управление, автоматический режим
- **Поддержка двух способов управления** Webasto:
  - Через реле (прямое подключение)
  - По K-Line протоколу (диагностическая шина)
- **Интеграция с платформами умного дома** через MQTT
- **Автоматическое обновление прошивки** через GitHub (AutoOTA)
- **Web-интерфейс** для настройки Wi-Fi (SimplePortal)
- **OLED-дисплей** для отображения текущего состояния
- **Энергосберегающий режим** с пробуждением по кнопке
- **Защита АКБ** от глубокого разряда и перезаряда

## 🔧 Аппаратные требования

### Микроконтроллер
- **ESP32** (любая версия с поддержкой Wi-Fi)

### Датчики температуры
- **2× DS18B20** (один для салона, один для антифриза)
- **Подключение**: GPIO16 и GPIO17

### Дисплей
- **OLED 128×64** с контроллером SSH1106 или SSD1306
- **Интерфейс**: I2C (SDA: GPIO32, SCL: GPIO25)

### Энкодер (для управления)
- **Rotary encoder** с кнопкой
- **Подключение**: CLK → GPIO35, DT → GPIO34, SW → GPIO33

### Управление Webasto
#### Вариант 1: Через реле (рекомендуется для простоты)
- **Реле управления питанием**: GPIO19
- **Реле вентилятора отопителя**: GPIO22
- **Реле климат-контроля**: GPIO18

#### Вариант 2: По K-Line (для продвинутых пользователей)
- **K-Line интерфейс**: RX → GPIO14, TX → GPIO34
- **Скорость обмена**: 10400 бод

### ШИМ-выходы
- **ШИМ помпы** (GPIO21, 1000 Гц)
- **ШИМ вентиля отопителя** (GPIO23, 400 Гц)

### Питание и мониторинг АКБ
- **Аналоговый вход измерения напряжения**: GPIO36
- **Делитель напряжения**: R1=4.635кОм, R2=990 Ом

## 🔌 Схема подключения
```text

┌─────────────────────────────────────────────────────────────┐
│ ESP32 │
│ │
│ DS18B20 (салон) ──── GPIO16 │
│ DS18B20 (антифриз) ─ GPIO17 │
│ OLED I2C SDA ─────── GPIO32 │
│ OLED I2C SCL ─────── GPIO25 │
│ Энкодер CLK ──────── GPIO35 │
│ Энкодер DT ───────── GPIO34 │
│ Энкодер SW ───────── GPIO33 │
│ K-Line RX ────────── GPIO14 (опционально) │
│ K-Line TX ────────── GPIO34 (опционально) │
│ Реле Webasto ─────── GPIO19 │
│ Реле вентилятора ─── GPIO22 │
│ Реле климата ─────── GPIO18 │
│ ШИМ помпа ────────── GPIO21 │
│ ШИМ вентиль ──────── GPIO23 │
│ Измерение АКБ ────── GPIO36 │
│ Светодиод статуса ── GPIO2 │
│ Зуммер ───────────── GPIO4 │
└─────────────────────────────────────────────────────────────┘
```

## 📥 Установка и настройка

### 1. Установка среды разработки

1. Установите **Arduino IDE** или **PlatformIO**
2. Добавьте поддержку плат ESP32 в менеджере плат
3. Установите необходимые библиотеки (см. список в разделе "Структура проекта")

### 2. Настройка проекта

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/eu1abg/Webasto_virtuino.git 2. Откройте проект в Arduino IDE или PlatformIO.
2. Откройте проект в Arduino IDE или PlatformIO.
3. Настройте параметры в коде:

а.Измените mqtt_server, mqtt_username, mqtt_password на свои.

б.При необходимости измените TIME_TO_SLEEP и TIME_POWER_SLEEP для настройки режимов сна.
### 3. Первый запуск
1. Прошейте ESP32
2. При первом включении устройство создаст точку доступа Wi-Fi:
   а. SSID: LabadaSto
   б. Пароль: 12345678
3. Подключитесь к этой сети и откройте браузер
4. Автоматически откроется портал настройки Wi-Fi (или перейдите по адресу 192.168.4.1)
5. Выберите свою Wi-Fi сеть и введите пароль
6. Устройство перезагрузится и подключится к указанной сети

## 🚀 Режимы работы

### 0: Все выключено
- Полное отключение всех систем
- Webasto и помпа выключены

### 1: Подготовка (5 минут)
- Включается только климат-контроль
- Webasto и помпа выключены
- Через 5 минут автоматическое отключение

### 2: Прогрев двигателя (15 минут)
- Webasto включен на полную мощность
- Помпа работает на 86% (ШИМ=220)
- Вентилятор отопителя выключен
- Через 15 минут автоматическое отключение

### 3: Ручное управление
- Ручной контроль температуры
- Webasto включается при температуре антифриза ниже 69°C
- Помпа управляется ПИД-регулятором для поддержания заданной температуры
- Максимальное время работы: 15 часов

### 4: Автоматический режим (рекомендуется)
- Автоматическое поддержание заданной температуры в салоне
- Два ПИД-регулятора:
  - Для управления заслонкой отопителя (температура в салоне)
  - Для управления помпой (температура антифриза)
- Интеллектуальное включение/выключение Webasto
- Максимальное время работы: 24 часа


## 📡 MQTT API

Устройство публикует данные и принимает команды через MQTT. Каждое устройство имеет уникальный ID на основе MAC-адреса.

### Топики для публикации (только чтение)
```
| Топик | Описание | Формат |
|-------|----------|--------|
| `{chipId}/web_RRSI` | Уровень сигнала Wi-Fi | Число (dB) |
| `{chipId}/web_ts` | Температура в салоне | Число (°C) |
| `{chipId}/web_ta` | Температура антифриза | Число (°C) |
| `{chipId}/web_vakb` | Напряжение АКБ | Число (В) |
| `{chipId}/web_shim21` | ШИМ заслонки отопителя (0-100%) | Число |
| `{chipId}/web_vklweb` | Состояние Webasto (0/1) | Число |
| `{chipId}/web_vklpomp` | Состояние помпы (0/1) | Число |
| `{chipId}/web_tust` | Заданная температура | Число (°C) |
| `{chipId}/web_shim2` | ШИМ помпы (0-100%) | Число |
| `{chipId}/web_stroka` | Текстовая строка состояния | Строка |
| `{chipId}/web_vkl1` | Текущий режим работы (0-4) | Число |
| `{chipId}/web_ventil` | Состояние вентиля (0/1) | Число |
```
### Топики для подписки (управление)
```
| Топик | Описание | Допустимые значения |
|-------|----------|---------------------|
| `{chipId}/web_vkl` | Выбор режима работы | 0, 1, 2, 3, 4 |
| `{chipId}/web_shim1` | Ручная установка ШИМ заслонки | 20-100 |
| `{chipId}/web_tust1` | Установка заданной температуры | 18-25 |
```
### Пример управления через MQTT

```bash
# Включить автоматический режим
mosquitto_pub -t "12345678/web_vkl" -m "4"
```
# Установить температуру 22°C
mosquitto_pub -t "12345678/web_tust1" -m "22"
```
# Ручная установка ШИМ заслонки на 50%
mosquitto_pub -t "12345678/web_shim1" -m "50"
```
## 🎛️ Меню устройства

Управление через энкодер:

1. **Короткое нажатие** - включение подсветки дисплея
2. **Длинное нажатие** - вход в меню
3. **Поворот энкодера** - выбор пункта меню
4. **Длинное нажатие в меню** - подтверждение выбора

### Пункты меню:

1. **Все выкл** - переключение в режим 0
2. **Подготовка** - переключение в режим 1 (5 минут)
3. **Прогр.двигла** - переключение в режим 2 (15 минут)
4. **Ручное упр.** - переключение в режим 3
5. **Авто.** - переключение в режим 4
6. **Уст.темп.АВто.** - настройка заданной температуры (18-25°C)
7. **ID** - отображение уникального ID устройства
8. **Reset** - перезагрузка ESP32

## 🔄 Автоматическое обновление

Система использует **AutoOTA** для автоматического обновления прошивки:

1. Проверяет наличие обновлений на GitHub
2. При наличии новой версии отображает информацию на дисплее
3. Автоматически загружает и устанавливает обновление
4. После установки перезагружается

**Репозиторий для обновлений**: `eu1abg/Webasto_virtuino`
## 📁 Структура проекта

### Основные файлы

- `LabadaSto.ino` - основной скетч с полной логикой работы
- `README.md` - эта документация

### Зависимости (библиотеки)

```
#include <AutoOTA.h>              // Автоматическое обновление
#include <WiFi.h>                 // Работа с Wi-Fi
#include <GyverPID.h>             // ПИД-регуляторы
#include <SimplePortal.h>         // Web-интерфейс настройки Wi-Fi
#include <EEPROM.h>               // Сохранение настроек
#include <PubSubClient.h>         // MQTT-клиент
#include <WiFiClient.h>           // Сетевой клиент
#include <NTPClient_Generic.h>    // Работа со временем
#include <DS18B20.h>              // Датчики температуры
#include <GyverOLED.h>            // Работа с OLED-дисплеем
#include "GyverEncoder.h"         // Работа с энкодером
#include <HardwareSerial.h>       // Последовательный порт для K-Line
#include <TimerMs.h>              // Таймеры
```
### Конфигурация K-Line протокола

Проект содержит готовые команды для работы с Webasto по K-Line:
```
byte Wakeup[] = {0x81, 0x51, 0xF1, 0xA1, 0x64};      // Пробуждение
byte Init1[] = {0x81, 0x51, 0xF1, 0x81, 0x44};       // Инициализация 1
byte Init2[] = {0x82, 0x51, 0xF1, 0x3C, 0x00, 0x00}; // Инициализация 2
byte Request1[] = {0x83, 0x51, 0xF1, 0x2A, 0x01, 0x01, 0xF1}; // Напряжение и температура
byte Request2[] = {0x83, 0x51, 0xF1, 0x2A, 0x01, 0x02, 0xF2}; // Статические данные
byte Request3[] = {0x83, 0x51, 0xF1, 0x2A, 0x01, 0x05, 0xF5}; // Версия прошивки
byte Request4[] = {0x83, 0x51, 0xF1, 0x31, 0x22, 0xFF, 0x17}; // Включение Webasto
byte Request5[] = {0x83, 0x51, 0xF1, 0x31, 0x22, 0x01, 0x19}; // Команда 5
byte Request6[] = {0x83, 0x51, 0xF1, 0x31, 0x22, 0x00, 0x18}; // Выключение Webasto
byte Request7[] = {0x83, 0x51, 0xF1, 0x2A, 0x01, 0x02, 0xF2}; // Диагностика
```
## Настройки по умолчанию

### Конфигурационные константы
```
#define Kline 0      // Использовать K-Line (1) или внешние датчики (0)
#define Rele 1       // Использовать реле (1) или K-Line (0) для управления
#define dshim1 5     // Смещение нуля ШИМ для заслонки
```
```
#define TIME_TO_SLEEP 3        // Время сна (секунды)
#define TIME_POWER_SLEEP 6     // Время работы перед сном (секунды)
```
```
// ПИД-регуляторы
GyverPID regulator1(0.5, 0.5, 0.1);  // Печка (Kp, Ki, Kd)
GyverPID regulator2(2, 1, 0.1);      // Помпа (Kp, Ki, Kd)
```
```
// Настройки MQTT
const char* mqtt_server = "m6.wqtt.ru";
const char* mqtt_username = "eu1abg";
const char* mqtt_password = "13051973";
const int mqtt_port = 14516;
```
### Защита АКБ
```
float v = 10.7;      // Нижний порог напряжения (отключение)
float v2 = 11.7;     // Верхний порог напряжения
float vakb;          // Текущее напряжение АКБ
int batlow = 0;      // Флаг низкого напряжения
```

## 🛠️ Настройка ПИД-регуляторов
Для оптимальной работы может потребоваться настройка ПИД-коэффициентов:
#### Регулятор заслонки отопителя (regulator1)
```
regulator1.setDirection(NORMAL);   // Прямое регулирование
regulator1.setLimits(20, 100);     // Ограничения ШИМ (20-100%)
regulator1.setpoint = 22;          // Заданная температура
```
#### Регулятор помпы (regulator2)

```
regulator2.setDirection(NORMAL);   // Прямое регулирование
regulator2.setLimits(75, 250);     // Ограничения ШИМ (75-250)
regulator2.setpoint = 22;          // Заданная температура
```
#### ⚠️ Важные примечания

Безопасность: Убедитесь в правильности подключения к автомобильной электросети
Предохранители: Используйте предохранители соответствующего номинала
Полярность: Соблюдайте полярность при подключении датчиков и исполнительных устройств
Изоляция: Надежно изолируйте все соединения в автомобиле
Тестирование: Протестируйте систему в безопасных условиях перед установкой в автомобиль

#### 📞 Поддержка и обратная связь
1. Автор: eu1abg
2. GitHub: https://github.com/eu1abg
3. Версия прошивки: 2.02

#### 📄 Лицензия
Этот проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE.

Важно: Данный проект предназначен для образовательных целей. Автор не несет ответственности за любые повреждения оборудования или другие последствия использования этого кода. Используйте на свой страх и риск.


